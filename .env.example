# ============================================================================
# DOCKER COMPOSE - VARIÁVEIS DE AMBIENTE
# ============================================================================
# Copie este arquivo para .env e ajuste os valores conforme necessário
# ============================================================================

# ============================================================================
# DJANGO BACKEND
# ============================================================================

# Segurança (OBRIGATÓRIO em produção)
# Gere uma chave segura com: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=change-me-to-a-secure-random-key-in-production

# Ambiente
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,backend,seu-dominio.com

# CORS e CSRF
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://seu-dominio.com

# ============================================================================
# BANCO DE DADOS (PostgreSQL)
# ============================================================================
DB_NAME=syspasso
DB_USER=postgres
DB_PASSWORD=change-me-to-a-secure-password
DB_HOST=db
DB_PORT=5432

# ============================================================================
# FRONTEND (Next.js)
# ============================================================================
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================
# 1. Este arquivo é um TEMPLATE - nunca commite o arquivo .env real
# 2. Em produção, use valores fortes para SECRET_KEY e DB_PASSWORD
# 3. Configure ALLOWED_HOSTS com seus domínios reais
# 4. Configure CSRF_TRUSTED_ORIGINS com suas URLs reais (com https://)
# 5. O NEXT_PUBLIC_API_BASE_URL deve apontar para o backend em produção

# ============================================================================
# SYS PASSO - CONFIGURAÇÕES DE AMBIENTE
# ============================================================================
#
# Este arquivo documenta todas as variáveis de ambiente necessárias.
# Copie este arquivo para .env e preencha com os valores reais.
#
# IMPORTANTE: Nunca commite o arquivo .env com dados sensíveis!
# ============================================================================

# ============================================================================
# SEGURANÇA - OBRIGATÓRIO
# ============================================================================

# SECRET_KEY - Chave secreta do Django (OBRIGATÓRIO)
# Gere uma nova chave com: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=your-secret-key-here-change-this-in-production

# ============================================================================
# AMBIENTE
# ============================================================================

# DEBUG - Modo de desenvolvimento/produção
# Valores aceitos: True, False, 1, 0, yes, no
# ATENÇÃO: SEMPRE use False em produção!
DEBUG=True

# ALLOWED_HOSTS - Hosts/domínios permitidos (separados por vírgula)
# Desenvolvimento: localhost,127.0.0.1
# Produção: seudominio.com,www.seudominio.com,api.seudominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================================================
# CSRF E CORS
# ============================================================================

# CSRF_TRUSTED_ORIGINS - Origens confiáveis para requisições CSRF (separadas por vírgula)
# Deve incluir o protocolo (http:// ou https://)
# Desenvolvimento: http://localhost:3000,http://127.0.0.1:3000
# Produção: https://seudominio.com,https://www.seudominio.com
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# CORS_ALLOWED_ORIGINS - Origens permitidas para CORS (separadas por vírgula)
# Deixe vazio para usar padrões de desenvolvimento
# Produção: https://seudominio.com,https://www.seudominio.com
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ============================================================================
# BANCO DE DADOS (OPCIONAL - usar quando migrar de SQLite)
# ============================================================================

# Para PostgreSQL (recomendado para produção):
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=syspasso
# DATABASE_USER=seu_usuario
# DATABASE_PASSWORD=sua_senha
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# Para MySQL:
# DATABASE_ENGINE=django.db.backends.mysql
# DATABASE_NAME=syspasso
# DATABASE_USER=seu_usuario
# DATABASE_PASSWORD=sua_senha
# DATABASE_HOST=localhost
# DATABASE_PORT=3306

# ============================================================================
# EMAIL (OPCIONAL - para envio de emails)
# ============================================================================

# Backend de email (desenvolvimento usa console)
# Produção: django.core.mail.backends.smtp.EmailBackend
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# Configurações SMTP (para Gmail, Outlook, etc.)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=seu_email@gmail.com
# EMAIL_HOST_PASSWORD=sua_senha_de_aplicativo

# ============================================================================
# REDIS E CELERY (OPCIONAL - para tarefas assíncronas)
# ============================================================================

# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0

# ============================================================================
# LOGGING (OPCIONAL)
# ============================================================================

# LOG_LEVEL=INFO
# LOG_FILE_PATH=/var/log/syspasso/django.log

# ============================================================================
# NOTAS DE PRODUÇÃO
# ============================================================================
#
# Antes de fazer deploy em produção, certifique-se de:
#
# 1. SEGURANÇA:
#    - Gerar uma nova SECRET_KEY forte e única
#    - Configurar DEBUG=False
#    - Configurar ALLOWED_HOSTS com seus domínios reais
#    - Configurar CSRF_TRUSTED_ORIGINS com suas URLs reais (HTTPS)
#    - Usar HTTPS em produção (certificado SSL/TLS)
#
# 2. BANCO DE DADOS:
#    - Migrar de SQLite para PostgreSQL ou MySQL
#    - Configurar backups automáticos
#
# 3. EMAIL:
#    - Configurar servidor SMTP real
#    - Usar credenciais seguras (senhas de aplicativo)
#
# 4. INFRAESTRUTURA:
#    - Configurar servidor web (Nginx, Apache)
#    - Configurar WSGI (Gunicorn, uWSGI)
#    - Configurar logs em produção
#    - Configurar monitoramento (Sentry, etc.)
#
# 5. CACHE E PERFORMANCE:
#    - Configurar Redis para cache e Celery
#    - Ativar compressão de arquivos estáticos
#    - Usar CDN para arquivos de mídia
#
# ============================================================================
